name: Test Environment Approval

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: Test-staging
    steps:
      - run: echo "If this runs, approval was granted"
