name: Test Environment Approval

on:
push:
branches:
- main

jobs:
test-environment:
runs-on: ubuntu-latest
environment: Test-staging

```
steps:
  - name: Test step
    run: echo "Environment approved and job started!"
```
