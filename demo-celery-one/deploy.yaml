apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-celery-one
  namespace: celery
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-celery-one
  template:
    metadata:
      labels:
        app: demo-celery-one
    spec:
      containers:
      - name: demo-celery-one
        image: 961328178101.dkr.ecr.ap-south-1.amazonaws.com/rounds-api-test:v0.0.1
        imagePullPolicy: IfNotPresent
